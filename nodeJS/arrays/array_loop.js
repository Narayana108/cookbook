// Easy way to loop through an array
array = ["vovlo", "bmw", "mclaren", "nissan"];

for (let i in array) {  
    console.log(i); // i = the position of the element in the array
    console.log(array[i]);
}